# Backend Configuration

## 🔧 Настройка Backend URL

### Переменные окружения

Создайте файл `.env` в корне проекта со следующими настройками:

```bash
# Backend API URL (required)
BACKEND_API_URL=https://chudov1she-multialtaibackend-8041.twc1.net/api

# Optional: Client-side backend URL
# If not set, client-side requests will use /api proxy (recommended)
# NEXT_PUBLIC_BACKEND_URL=https://chudov1she-multialtaibackend-8041.twc1.net/api
```

### Что изменится

1. **API-запросы** будут идти на URL из `BACKEND_API_URL`
2. **Серверный код** использует `BACKEND_API_URL` напрямую
3. **Клиентский код** использует `/api` (проксируется через Next.js API routes) или `NEXT_PUBLIC_BACKEND_URL` если задан
4. **Переменная обязательна** - без `BACKEND_API_URL` приложение не запустится

## 🚀 Сборка приложения

### Команды сборки

```bash
# Development build
npm run build

# Production build
npm run build

# Start production server
npm run start
```

### Где собирается приложение

Приложение собирается в папку `.next/` в корне проекта:

```
frontend/
├── .next/           # ← Собранное приложение
├── src/             # ← Исходный код
├── public/          # ← Статические файлы
├── package.json
└── ...
```

### Что происходит при сборке

1. **TypeScript компилируется** в JavaScript
2. **React компоненты** оптимизируются
3. **CSS минифицируется** и оптимизируется
4. **Изображения** оптимизируются через Next.js Image
5. **API-роуты** компилируются
6. **Статические файлы** копируются в `.next/`

### Деплой

После сборки папка `.next/` содержит все необходимое для запуска:

```bash
# Копируем .next/ на сервер
# Запускаем
npm run start
```

### Переменные окружения на сервере

Не забудьте настроить переменную окружения на продакшен сервере:

```bash
# На сервере
export BACKEND_API_URL=https://chudov1she-multialtaibackend-8041.twc1.net/api
```

Или создайте файл `.env` в корне проекта с тем же содержимым.
