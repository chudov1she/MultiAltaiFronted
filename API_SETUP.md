# API Setup для Frontend

## 🎯 Что изменилось

Упрощена архитектура API - убрана сложная логика с nginx и разными URL для сервера/клиента.
Созданы конкретные API-роуты для проксирования запросов на backend.
Все URL берутся из переменных окружения в файле `.env`.

## 🔧 Настройка

### Переменные окружения

Создайте файл `.env` в корне проекта:

```bash
# Backend API URL (required)
BACKEND_API_URL=https://chudov1she-multialtaibackend-8041.twc1.net/api

# Optional: Client-side backend URL
# If not set, client-side requests will use /api proxy (recommended)
# NEXT_PUBLIC_BACKEND_URL=https://chudov1she-multialtaibackend-8041.twc1.net/api
```

### Backend URL
- **Серверные компоненты**: используют `BACKEND_API_URL` из `.env` напрямую
- **Клиентские компоненты**: используют `/api` (проксируется через API-роуты) или `NEXT_PUBLIC_BACKEND_URL` если задан

### Проксирование
Созданы конкретные API-роуты:
- `src/app/api/v1/catalog/land-plots/route.ts` - для земельных участков
- `src/app/api/v1/catalog/property-types/route.ts` - для типов недвижимости
- `src/app/api/v1/catalog/properties/route.ts` - для generic properties
- `src/app/api/[...path]/route.ts` - catch-all для остальных endpoints

## 📍 API Endpoints

Все эндпоинты начинаются с `/api/v1/`:

- **Каталог**: `/api/v1/catalog/...`
- **Листинги**: `/api/v1/listings/...`
- **Контакты**: `/api/v1/contacts/...`
- **Новости**: `/api/v1/news/...`
- **Квизы**: `/api/v1/quizzes/...`
- **Заявки**: `/api/v1/requests/...`

## 🚀 Запуск

1. Создайте файл `.env` в корне проекта с переменной `BACKEND_API_URL`
2. **Перезапусти frontend** (чтобы изменения вступили в силу)
3. Все API-запросы будут работать корректно:
   - Серверные компоненты → используют `BACKEND_API_URL` напрямую
   - Клиентские компоненты → `/api` (проксируется через API-роуты)

## 📝 Примеры URL

- **Серверные компоненты**: используют `BACKEND_API_URL` из `.env`
- **Клиентские компоненты**: `/api/v1/catalog/land-plots/` (проксируется через API-роут)

## 🔍 Проверка

Открой DevTools → Console и посмотри логи API-запросов:
- Серверные запросы идут напрямую на `BACKEND_API_URL`
- Клиентские запросы идут на `/api` и проксируются через API-роуты
- В консоли должны появиться логи `[Land Plots API]`, `[Property Types API]` и т.д.
